#!/bin/sh
echo "Starting to update website"
git add .
git commit -m "Updating my website"
git push -u origin master
hugo
echo "Updated website, now pushing to the web"
cd public
git add .
git commit -m "Building website"
git push origin master
cd ..
echo "Website should be updated"
